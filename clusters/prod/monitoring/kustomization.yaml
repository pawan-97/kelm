apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

# Reference the single flat kube-prometheus-stack definition
resources:
  - ../../../apps/kube-prometheus-stack

commonLabels:
  cluster: prod
